node_modules/
dist/
.astro/

# The gallery is a separate git worktree tracking the `gallery` branch,
# so that deleted photos never linger in `main`'s history.
/gallery/

# Scratch space for testing the decoder against real camera files.
/samples/

# Local machine state for the studio uploader.
/studio/.state/
